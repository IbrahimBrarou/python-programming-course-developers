# c, c+, Java are static which means it is needed to declare a type for variables. type of variables is deterrmened at compile time
# Ruby, Python are dynamic. type of variable is determined at runtime
students_count = 1000
print(type(students_count))
